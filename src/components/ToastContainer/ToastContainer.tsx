import 'react-toastify/dist/ReactToastify.min.css';

import React from 'react';

import * as S from './ToastContainer.styled';

const ToastContainer = () => <S.ToastContainer />;

export default ToastContainer;
